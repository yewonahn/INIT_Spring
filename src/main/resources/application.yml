spring:
  datasource:
    url: jdbc:mysql://localhost:3306/study-init?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: qnfleld8u!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
  mvc:
    pathmatch:
      matching-strategy : ant_path_matcher